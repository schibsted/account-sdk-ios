source 'https://cdn.cocoapods.org'

platform :ios, '11.0'
use_frameworks!

pod 'SwiftLint'

target 'SchibstedAccount' do
    target 'SchibstedAccountTests' do
        inherit! :search_paths
		pod 'Quick'
		pod 'Nimble'
    end
end
